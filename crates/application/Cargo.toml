[package]
name = "application"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
domain = { path = "../domain" }
storage = { path = "../storage" }
platform_windows = { path = "../platform_windows" }
