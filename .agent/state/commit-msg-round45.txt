fix(campaign): re-measure the 484-capability rows whose recorded gaps had closed

Round 45 audit of the campaign dimension. Seven CASE_RESULTS rows still described
gaps that later rounds closed, which is stale accounting of exactly the kind
DOD-026/DOD-032 forbid. Each was re-measured against current evidence, and rows
whose gap is genuinely open were restated precisely or left alone.

Now PASS with the closing evidence named:
- SUP-001 (14 of 14 namespaces implemented, vault write path wired, UI invokes the
  backend, 0 UNREACHABLE functions) -- was "2 of 14 namespaces, persisted=false".
- SUP-003 (SBOM 720 components + gate, THIRD_PARTY_NOTICES, anti-gaming and secret
  scans, install/read-back/uninstall, signing DECIDED by ADR-003 rather than
  missing) -- was four named gaps.
- E2E-004 (the artifact lane drives the packaged binary over real IPC: envelope,
  correlation ids, SPEC-006 classes, scope, diagnostics, ledger, keyed writes with
  replay and refusal, provider identity, audited confirmation, canary read back --
  42 assertions) -- was "no suite exercises the real IPC boundary".
- E2E-012 (process kill + relaunch + read back through the runtime; migration
  matrix over five prior schema states) -- was "single-process reopen, DOD-016
  untested".
- E2E-002 / E2E-003 (browser lane grew from 5 and 11 tests to 22, covering the
  functional surfaces; the state-changing golden path is driven in the
  exact-artifact lane, which is where the Tauri bridge exists).
- SUP-006 (idempotency keys, replay/conflict, retry/reorder/delayed/concurrent,
  zero lost events under stress) -- was "no idempotency keys at the API boundary".

Still PARTIAL, deliberately:
- SUP-004 (reproducible build: WiX ProductCode and PE timestamps differ per build;
  measured and documented, not solved).
- E2E-011 (not a virgin clean room -- DOD-034 external).
- GEN-016 (restated: database/policy/provider negative paths are executed and
  mutation-proven, and there is no AUTH boundary to exercise in a local-first
  single-user app, so that half is NOT_APPLICABLE rather than absent).

Also fixed a flaky assertion the lane surfaced: the stress trial failed in the
unit lane with "1 concurrent backup(s) failed" and no message. Backup failures are
now classified and their messages recorded, applying the trial's own stated policy
-- a busy/locked failure is counted and reported as a locking fact for backups
exactly as it already was for writes, while every other failure still fails the
trial.

Registry accounting: PASS 7, PARTIAL 3, EXTERNAL_REQUIRED 1,
NOT_RUN_BLOCKED_MATERIAL 473 of 484. verify.sh: ok.
