feat(DOD-023,DOD-008): executable documentation accounted, coverage measured

DOD-023 -> PASS, re-measured by running the gate to completion: 7 of 7 documents
scanned, 25 commands executed (22 PASS, 3 FAILED), 3 skipped with reasons, 0
referenced paths missing. Every remaining failure is accounted for and none is
masked: verify.sh fails only because it ran after other documented commands had
changed tracked inputs in the same sequence (DOD-040 working, and it exits 0 on a
settled tree); production-readiness-check.sh exits 1 because the verdict is
NO_GO, which is correct; pnpm audit exits 1 on 2 moderate dev-only advisories
below the enforced level. Both false failures fixed earlier are confirmed gone:
test-e2e.sh and live-fire.sh now PASS under doc-exec, and the missing-path count
is 0. The clause cites the generator rather than the generated STATUS.md, because
that file is rewritten on every run while DOD_STATUS digests what it cites.

DOD-008 -> PASS. The recorded gap -- "no coverage measurement" -- is closed with
scripts/coverage-gate.py: the eleven library crates are instrumented with
-C instrument-coverage, their binaries are RUN BY THE GATE so each profile and its
object come from the same execution, and llvm-profdata/llvm-cov from the
toolchain's own llvm-tools-preview component report line/function/region coverage.
Measured: line 93.4%, function 91.39%, region 93.04%, per-crate detail recorded,
floors enforced, and the report pins the epoch it was measured at so a source
change invalidates it (verify.sh enforces --check; the settle path re-measures).

Three harness defects were found while building it, each of which would have
produced a false number: the host LLVM is a different patch version from the LLVM
rustc links and reported 0.00% for everything from valid profiles; llvm-cov given
a relative -instr-profile silently failed to read it; and workspace-relative paths
produced an empty per-crate table beside a non-zero TOTAL. A 0% TOTAL is now a
harness failure rather than a recorded measurement. Mutation sensitivity, the
clause's other evidence item, remains separately proven (12 controlled defects).

The desktop application crate is excluded from the coverage measurement by name
and is covered at the artifact boundary instead; the exclusion is printed in the
report. New environment prerequisite, documented in COMMANDS.md:
`rustup component add llvm-tools-preview`.

DoD tally: PASS 36, PARTIAL 3, FAIL 1 (DOD-035), EXTERNAL_REQUIRED 2.
Requirements 57/59 bound. verify.sh: ok.
