# Verification Accounting Status (honest, measured)

Measured 2026-09-10 at candidate `5ea1df3`.

## Headline

```
$ sh scripts/harness-accounting.sh
registry 484 accounted 484
[exit=0]

$ python3 scripts/build-accounting.py --check
accounting check: ok (484 IDs, one status each)

$ python3 scripts/build-accounting.py
  NOT_RUN_BLOCKED_MATERIAL: 484
```

**All 484 registry IDs now carry exactly one accounted status** (DOD-030
invariant: 484 rows, 484 unique, 0 missing, 0 duplicated). At base revision this
was `registry 484 accounted 0`, exit 2.

**Read this correctly: 484/484 accounted is NOT 484/484 passing.** Every ID is
currently `NOT_RUN_BLOCKED_MATERIAL`, which means: one honest status each, and
**zero IDs have evidence-backed PASS**. No candidate-specific case material has
been executed for any registry ID. DOD-030 requires the *accounting*; it does not
claim the *verification*. Both facts are stated here so neither is laundered.

Accounting is generated by `scripts/build-accounting.py`, which refuses to
synthesize PASS, validates every status against the DOD-032 taxonomy, rejects
duplicate/unknown IDs, and asserts the registry is still exactly 484.

## Supporting state

| Artifact | Status |
| --- | --- |
| `.agent/verification/reports/COMPLETE_TEST_ACCOUNTING.csv` | 484 rows, 484 unique IDs |
| `.agent/verification/state/TEST_LEDGER.jsonl` | 484 entries |
| `.agent/verification/state/CASE_RESULTS.json` | 0 executed cases (empty `results`) |
| `.agent/verification/state/DOD_STATUS.jsonl` | still empty — DoD dispositions pending |
| `.agent/verification/reports/RELEASE_GATE.json` | `NOT_EVALUATED` |
| `.agent/verification/state/RUN_STATE.json` | `NOT_STARTED` |

## Registry composition (484 rows verified present)

| Source group / kind | Count |
| --- | --- |
| Blockchain, atomic-security | 202 |
| HIPAA, atomic-security | 125 |
| General, atomic-security | 122 |
| E2E, orchestrator | 20 |
| Supplemental, production-gate | 15 |
| **Total** | **484** |

Applicability: 449 of 484 rows are `conditional*`/`evaluate`, so DOD-041
requires an evidence-attached applicability decision for each. None exists yet.

## Verified-green capabilities at this candidate

These were executed, not assumed:

| Gate | Command | Result |
| --- | --- | --- |
| Preflight | `sh scripts/preflight.sh` | `preflight: ok`, exit 0 |
| Pack validation | `python3 scripts/validate-generated-pack.py .` | `ok`, exit 0 (was FAILED) |
| Accounting | `sh scripts/harness-accounting.sh` | 484/484, exit 0 (was 0/484, exit 2) |
| Rust build | `cargo build --workspace` | success, exit 0 (was RC2175, exit 101) |
| Rust tests | `cargo test --workspace --locked` | 25 suites, **49 passed, 0 failed, 0 ignored**, exit 0 |
| Rust lint | `sh scripts/lint.sh` (`clippy -D warnings`) | exit 0 (was exit 101) |
| Format | `sh scripts/format-check.sh` | exit 0 (was exit 1) |
| Typecheck | `sh scripts/typecheck.sh` | exit 0 |
| Windows bundle | `sh scripts/build.sh` / `tauri build` | MSI + NSIS, exit 0 |
| App boot | launch `linchpin-desktop.exe` | window "LINCHPIN Patent Intelligence OS", responding |
| cargo-deny advisories | `cargo deny check advisories` | `ok`, exit 0 (was FAILED) |
| cargo-deny bans | `cargo deny check bans` | `ok`, exit 0 (was FAILED) |

## Verified-red capabilities at this candidate

| Gate | Command | Result | Cause |
| --- | --- | --- | --- |
| Licenses | `cargo deny check licenses` | FAILED, exit 4 | 5 MPL-2.0 transitive crates need the legal review ADR-002 requests |
| Production readiness | `sh scripts/production-readiness-check.sh` | exit 1 | release verdict is not GO (correct — no artifact verdict exists) |
| Integration | `sh scripts/test-integration.sh` | exit 2 | `run-integration-real.sh` absent |
| E2E | `sh scripts/test-e2e.sh` | exit 1 | no package defines a `test:e2e` script |
| Smoke | `sh scripts/smoke-test.sh` | exit 2 | exact-artifact smoke runner absent (EP-009 M3) |
| Live fire | `sh scripts/live-fire.sh` | exit 2 | `run-live-fire-real.sh` absent |
| Reality gate | `sh scripts/reality-gate.sh` | exit 1 | anti-gaming matches require classification (now documented) |

These are honest gaps, not failures to hide. Each is a real missing artifact.

## What is genuinely true today

- 11 library crates + 1 Tauri desktop app compile from a clean checkout with the pinned toolchain (rustc/cargo 1.98.0, `x86_64-pc-windows-msvc`).
- 49 Rust unit tests pass with zero ignored and zero skipped.
- Two real Windows installers (MSI, NSIS) build and carry correct product identity, verified through the Windows Installer COM API.
- The desktop app boots and renders.
- Domain logic (state machines, claim graph, docket lifecycle), storage (SQLite, vault, Tantivy), evidence firewall, patent, commercialization, research and MCP crates are real, tested code.
- Three fabrication defects were found and repaired with mutation proofs (see `ANTI_GAMING_FINDINGS.md`).

## What is not true

- No registry ID has evidence-backed PASS; all 484 are `NOT_RUN_BLOCKED_MATERIAL`.
- No requirement→test traceability rows exist; `REQ-REL-001`/`REQ-REL-005` are still defined nowhere (DOD-001 gap).
- No DoD clause has an evidence-backed disposition (`DOD_STATUS.jsonl` empty).
- No SBOM, notices, provenance or signing (EP-009 M2/M4).
- No clean-room Win10/11 install, no UAT, no accessibility sign-off (M3, PF-016/017).
- No provider live-fire; `provider_transport` is now honest but unreachable from the desktop boundary.
- No release verdict; `RELEASE_GATE.json` remains `NOT_EVALUATED`.
