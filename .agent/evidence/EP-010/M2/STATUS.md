# EP-010 / M2 — Account all 484 registry IDs and all 42 DoD clauses

Status: **DONE — both accounting axes are complete, machine-validated, currency-checked,
and contain no missing or duplicated ID.**

## 484 registry IDs (DOD-030)

| Property | Measured |
| --- | --- |
| Registry rows | 484 (`MASTER_TEST_REGISTRY.csv`) |
| Accounted rows | 484, 484 unique IDs, 0 missing, 0 duplicated (`COMPLETE_TEST_ACCOUNTING.csv`) |
| Applicability decisions | 484: APPLICABLE 74, NOT_APPLICABLE 410 (`APPLICABILITY_MATRIX.csv`) |
| Final statuses | PASS 58, EXTERNAL_REQUIRED 7, NOT_RUN_BLOCKED_MATERIAL 4, PARTIAL 4, DEFERRED_LONG_RUNNING 1, NOT_APPLICABLE 410 |

Guards that make those numbers mean something:

- `scripts/build-accounting.py` refuses a status outside the DOD-032 taxonomy, refuses
  a case result for a NOT_APPLICABLE ID, and asserts the count invariant.
- `scripts/validate-generated-pack.py` rejects duplicate probe keys — added after a
  measured defect in which three of four reclassifications were silent no-ops because
  a later duplicate key in the same dict literal won.
- `scripts/applicable-case-evidence.py --check` re-reads every **quoted line** of every
  citation in the applicable-case evidence (215 citations) against the artifact it
  claims, so a citation cannot go stale silently.
- `scripts/harness-accounting.sh` plus `scripts/ship-gate.py --check` assert the totals.

Readable summary: `.agent/verification/state/ACCOUNTING_STATUS.md`.
Per-stage view: `.agent/verification/reports/STAGE_ACCOUNTING.md`.

## 42 DoD clauses (DOD-032, DOD-042)

| Property | Measured |
| --- | --- |
| Clauses | 42, each with exactly one disposition (`DOD_STATUS.jsonl`) |
| Dispositions | PASS 38, EXTERNAL_REQUIRED 3 (DOD-001, DOD-034, DOD-039), DEFERRED_LONG_RUNNING 1 (DOD-038) |
| FAIL / PARTIAL / ERROR / UNVERIFIED / MISSING | 0 |

Relationship to the 484 registry IDs: the clause dispositions are not a summary of the
registry tally. The registry answers "was each capability case accounted for"; the
clauses answer "does the repository satisfy this rule". `DOD-030` (all IDs accounted)
is PASS while individual IDs are PARTIAL — both statements are true and the report
keeps them separate rather than letting one mask the other.

## Where the two axes meet

`.agent/verification/reports/RELEASE_GATE.json` (generated by `scripts/ship-gate.py`)
carries the clause tally, the registry tally, the artifact digests and the verdict
together, so a reader cannot quote one without the other. Its current verdict is
`CONDITIONAL_EXTERNAL_GATES` with **no blocking clause**.

## Claim discipline

`DOD-030`'s PASS means *accounting* is complete, not that every capability passed:
4 IDs are PARTIAL, 4 are NOT_RUN_BLOCKED_MATERIAL, 7 EXTERNAL_REQUIRED and 1
DEFERRED_LONG_RUNNING. Every one of them is enumerated with its reason and owning
stage in `.agent/verification/reports/RESIDUAL_RISK_AND_EXTERNAL_GATES.md`.
