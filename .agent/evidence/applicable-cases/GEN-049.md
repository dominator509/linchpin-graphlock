# GEN-049 -- Input Validation Testing

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/test-unit exists (CommandError::Validation input rejection); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- architecture probe -- `concern[validation]_tests: 23` (captured in `probes.log`)
- first-party source `crates/commercialization/src/asset_readiness.rs` -- quoted and verified: 'test_invalid_records_are_refused'
- first-party source `crates/evidence/src/lib.rs` -- quoted and verified: 'sanitize_path'

## What this establishes

Input validation executed: 23 boundary/invalid-input tests across the domain, commercialization, evidence and IPC layers, covering empty values, unknown identities, blank references, malformed dates, invalid record kinds and hardened paths.

## What this does not establish

Validation depth is per-command and typed (SPEC-006 classes) rather than driven by a schema-fuzzing harness; there is no property-based generator over the full command surface.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
