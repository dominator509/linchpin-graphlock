# GEN-051 -- Authorization Testing

**Status: NOT_RUN_BLOCKED_MATERIAL** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/test-unit exists (WorkspaceScope path-escape authorization); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- architecture probe -- `auth_surface_hits: 0` (captured in `probes.log`)
- architecture probe -- `mcp_capability_grant_hits: 11` (captured in `probes.log`)
- architecture probe -- `disclosure_firewall_hits: 42` (captured in `probes.log`)

## What this establishes

Measured absence of the subject of the test, with both nearest real controls measured present.

## Named blocking material

an authorization decision to test. A measured scan of all first-party source finds ZERO login/logout/authenticate/session-token/is-admin/role-based-access constructs: this is a local-first single-user desktop application with no caller identity, no session, no roles and no remote API, so there is no subject/object/permission triple for an authorization test to exercise. The nearest real control is the MCP capability grant (11 sites) and the disclosure firewall (42 sites), which are recorded under GEN-052. The registry pack declares this ID applicable to applications in general; the material an authorization test requires is absent from this architecture rather than missing from the harness.

## What this does not establish

No authorization test was executed, because executing one would require inventing an identity model the product does not have.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
