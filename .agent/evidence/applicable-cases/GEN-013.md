# GEN-013 -- Security Code Metrics Analysis

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/code-metrics exists (security code metrics: unsafe blocks, production panicking calls, suppressions, hotspot ranking); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- first-party source `scripts/code-metrics.py` -- quoted and verified: 'strip_test_modules', 'UNSAFE_ALLOWLIST', 'MAX_PRODUCTION_PANICS'
- evidence artifact `.agent/evidence/code-metrics/STATUS.md` -- quoted and verified: 'Counts', 'Complexity hotspots', 'Discrimination'
- evidence artifact `.agent/evidence/code-metrics/report.json` -- quoted and verified: 'totals', 'breaches', 'thresholds'

## What this establishes

Security code metrics now run, which this row denied. scripts/code-metrics.py measures, per function and per file over first-party source: cyclomatic complexity, `unsafe` blocks, panicking calls (`unwrap`/`expect`/`panic!`) split between production paths and test code, `todo!`/`unimplemented!` on production paths, and `#[allow(...)]` suppressions. ENFORCED, not decorative: complexity ceiling 40 per production function, production panicking calls capped at 15 (a ratchet), `unsafe` blocks allowed only for the single allowlisted Win32 FFI call, and zero `todo!`/`unimplemented!` in production. MEASURED on this candidate: 410 functions (353 production, max complexity 35; 57 in test files, max 43, exempt because a stress or soak trial is a long procedure by design), 7 production panicking calls, 1 unsafe block (the allowlisted GetProcessMemoryInfo call), 0 production todos, 5 suppressions. The report carries a ranked hotspot list so the number is actionable: assess_asset_readiness (35), check_support_matrix (27), apply_research_action (24).

## What this does not establish

Complexity here is decision-token counting, not dataflow analysis: it ranks functions for review and enforces a ceiling, and is blind to nesting depth, state space and architecture. The panic count does not distinguish an infallible unwrap from a load-bearing one, and no defect-density or security-debt metric exists.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
