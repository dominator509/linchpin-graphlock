# GEN-082 -- Pre-Commit Security Testing

**Status: NOT_RUN_BLOCKED_MATERIAL** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/harness-validate exists (pre-commit gate set); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- architecture probe -- `git_hooks_active: 0` (captured in `probes.log`)
- architecture probe -- `hook_framework[.husky]: False` (captured in `probes.log`)
- architecture probe -- `hook_framework[.pre-commit-config.yaml]: False` (captured in `probes.log`)
- architecture probe -- `root_package_husky_field: False` (captured in `probes.log`)

## What this establishes

Measured absence of any installed git hook or hook framework, with the equivalent later-stage lanes recorded under GEN-001/GEN-005.

## Named blocking material

a pre-commit boundary to exercise. Measured: `.git/hooks` contains ZERO active hooks (only `.sample` files) and no hook framework is configured (no .husky, lefthook, pre-commit config or husky field in the root package manifest). The same checks -- secret scan, lint, format, typecheck -- run one step later as enforced gate lanes, and that is recorded rather than used to claim this ID: testing a pre-commit hook requires a hook to exist, and installing one would change the developer workflow for every future candidate without owner authorization.

## What this does not establish

No pre-commit hook was tested, because none is installed.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
