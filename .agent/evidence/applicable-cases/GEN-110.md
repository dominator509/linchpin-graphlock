# GEN-110 -- Security Regression Testing

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/test-unit exists (regression suite over the production crates); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- evidence artifact `.agent/evidence/mutation-proof/REPORT.json` -- quoted and verified: 'MUT-OPS-037'
- first-party source `scripts/change-invalidation.py` -- quoted and verified: 'changed_classes'
- evidence artifact `.agent/evidence/coverage/SUMMARY.md` -- quoted and verified: 'Epoch:'

## What this establishes

Security regression testing executed by three mechanisms together: the mutation harness re-verifies that each guard still discriminates, the coverage gate records the epoch so a source change invalidates the measurement, and the change-invalidation graph re-runs every affected stage after a change.

## What this does not establish

Regression sensitivity is bounded by the mutations listed (thirteen); a control with no mutation would regress silently, and the harness does not enumerate missing mutations per control.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
