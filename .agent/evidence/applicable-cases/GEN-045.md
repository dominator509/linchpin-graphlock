# GEN-045 -- Injection Testing

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/live-fire exists (input validation rejection at command boundary); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- first-party source `crates/evidence/src/lib.rs` -- quoted and verified: 'sanitize_path', 'test_archive_entry_rejects_zip_slip'
- architecture probe -- `concern[traversal]_tests: 3` (captured in `probes.log`)
- first-party source `crates/provider_transport/src/lib.rs` -- quoted and verified: 'test_local_adapter_rejects_non_loopback_endpoint'
- evidence artifact `.agent/evidence/fuzz/STATUS.md` -- quoted and verified: 'fuzz_campaign.rs'

## What this establishes

Injection testing executed at the boundaries this product has: path/zip-slip hardening with a generated-corpus property test, fuzzed text parsers, punctuation-quoted credential forms in the redactor, and the local provider refusing a non-loopback endpoint.

## What this does not establish

There is no SQL-injection or HTML-injection scan by a dedicated tool; the datastore is accessed through parameterised statements and the UI through React, both covered by tests rather than by an injection scanner.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
