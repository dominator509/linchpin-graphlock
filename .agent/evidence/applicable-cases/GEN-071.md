# GEN-071 -- Sandbox and Isolated Environment Security Testing

**Status: NOT_RUN_BLOCKED_MATERIAL** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/smoke-test exists (isolated process execution is exercised by the smoke lane); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- architecture probe -- `sandbox_primitive_hits: 8` (captured in `probes.log`)
- architecture probe -- `sandbox_artifact[Dockerfile]: False` (captured in `probes.log`)
- architecture probe -- `sandbox_artifact[.devcontainer]: False` (captured in `probes.log`)
- architecture probe -- `sandbox_artifact[k8s]: False` (captured in `probes.log`)

## What this establishes

Measured absence of any isolation primitive or container artifact in the repository.

## Named blocking material

an isolation boundary to test. The product ships as a per-user Windows application with no container, job object, AppContainer, seccomp profile or VM boundary: no Dockerfile, devcontainer, kubernetes manifest or container runtime exists in the repository, and the only `sandbox` symbol in first-party source is a test double for git integration (`GitIntegrationSandbox`), which isolates nothing. The isolation controls that DO exist are recorded under their own IDs (loopback-only provider transport, device-local storage, clean-build environment isolation under DOD-002/DOD-005). Executing this capability would require building a sandboxed runtime the architecture does not have, not enabling an existing test.

## What this does not establish

No sandbox escape or confinement test was executed, because there is no confinement to escape.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
