# GEN-092 -- Attack Tree Analysis

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/check-attack-trees exists (attack trees bound to the threat model (leaf symbols, residuals, per-threat coverage)); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- first-party source `scripts/check-attack-trees.py` -- quoted and verified: 'MITIGATED leaf must name at least one', 'traceable to the threat model'
- evidence artifact `.agent/evidence/attack-trees.md` -- quoted and verified: '## AT-1', '## AT-2', '## AT-3', 'Unmitigated or partial leaves'
- evidence artifact `.agent/verification/state/ATTACK_TREES.json` -- quoted and verified: 'threat_notes', 'goal-first attack trees'
- evidence artifact `.agent/verification/state/THREAT_MODEL.json` -- quoted and verified: 'accepted_risks'

## What this establishes

Attack-tree analysis is authored AND bound: three goal-first trees (exfiltrate invention content; destroy or forge the conception record; get a tampered build installed) decompose into 14 leaves, of which 10 are MITIGATED with a symbol that must exist in the file it cites and 4 are OPEN or PARTIAL with a residual that must be traceable to the threat model's risk register. The validator fails on a tree naming an undeclared threat, a malformed node graph (missing children, orphans, cycles, non-leaf with fewer than two alternatives), a MITIGATED leaf with no bound symbol, an OPEN leaf with no residual, an untraceable residual, or a modelled threat that is neither represented by a tree nor given an explicit coverage note. Every one of the threat model's 11 threats is accounted for that way, so the two artefacts cannot drift apart.

## What this does not establish

Trees are a design artefact, not an exercise: nothing here was validated by an adversary, and 4 of the 14 leaves are deliberately left OPEN or PARTIAL (unbuilt credentialed lane, signed-release absence, unlabelled secret material, no OS-encrypted container). GEN-018 (manual penetration test) remains EXTERNAL_REQUIRED.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
