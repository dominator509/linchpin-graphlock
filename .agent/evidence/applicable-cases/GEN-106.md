# GEN-106 -- Property-Based Security Testing

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/test-unit exists (property tests over generated inputs: generated path corpus plus the mutation-fuzz campaign); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- first-party source `crates/evidence/src/lib.rs` -- quoted and verified: 'test_sanitize_path_property_over_generated_corpus', '20_000'
- first-party source `apps/desktop/src-tauri/tests/fuzz_campaign.rs` -- quoted and verified: 'Deterministic adversarial', 'fn mutate('
- evidence artifact `.agent/evidence/fuzz/STATUS.md` -- quoted and verified: '4 634 inputs per target'
- first-party source `crates/crash_reporter/src/lib.rs` -- quoted and verified: 'test_redaction_survives_case_folding_characters'

## What this establishes

Property-based security testing IS executed, which this row denied. Two generated-input harnesses run: (1) a seeded property test over a GENERATED path corpus -- 20 000 candidates assembled from an adversarial alphabet ('.', '..', '/', '\\', '%2e', '~', '..\\', ':' ...) asserting the invariant that sanitize_path never panics and rejects every path that resolves to an escape or an absolute root, with counters proving the corpus actually contained traversals; and (2) the fuzz campaign's deterministic adversarial phase (every awkward character at every position of every seed, 4 634 inputs per target across ten IPC-reachable parsers) plus its seeded mutation phase, both asserting the no-panic property. The mutation harness then proves the properties are discriminating by removing the guards they protect.

## What this does not establish

The generators are hand-rolled (a seeded xorshift64*), not proptest/quickcheck: there is no automatic shrinking outside the fuzz campaign's, no coverage-guided search, and the corpora are bounded by construction. The invariants asserted are the ones written down here; nobody proved the set is complete.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
