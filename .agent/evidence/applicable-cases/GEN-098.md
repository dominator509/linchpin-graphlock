# GEN-098 -- Security Control Verification

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/test-unit exists (security controls (scope guard, redaction) verified by unit tests); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- evidence artifact `.agent/evidence/mutation-proof/REPORT.json` -- quoted and verified: 'MUT-OPS-037', 'MUT-FUZZ-001', 'restored'
- first-party source `crates/storage/src/vault.rs` -- quoted and verified: 'test_migration_matrix_preserves_data_from_every_prior_schema'
- first-party source `crates/evidence/src/lib.rs` -- quoted and verified: 'test_archive_entry_rejects_zip_slip'

## What this establishes

Security control verification executed by controlled defect: thirteen mutations are applied to the controls that matter (credential redaction, traversal guard, truth-boundary guard, keyed-write replay/conflict detection, schema verification, ledger read, diagnostics recording, and the fuzz campaign itself), each caught by its guarding test, restored byte-for-byte and rerun green.

## What this does not establish

Two further mutations could not be shown to be discriminating and were WITHDRAWN rather than reported as caught, so the proof covers thirteen controls, not every control in the product.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
