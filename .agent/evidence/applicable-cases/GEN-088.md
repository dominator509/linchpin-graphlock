# GEN-088 -- Security Test Automation

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: probe names scripts/harness-run-stage for 'Security Test Automation' (harness stage automation) but that entry point cannot execute a case -- it refuses unconditionally or delegates to a runner absent from the tree.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- first-party source `scripts/rerun-invalidated.py` -- quoted and verified: 'NO_RUNNER', 'digest'
- evidence artifact `.agent/evidence/coverage/SUMMARY.md` -- quoted and verified: 'Generated by `scripts/coverage-gate.py`'
- architecture probe -- `concern[concurrency]_tests: 5` (captured in `probes.log`)

## What this establishes

Security test automation executed: every stage is mapped to a concrete repository command, a stage whose runner is missing is reported NO_RUNNER rather than silently skipped, and the commands run with recorded exit codes and output digests; the coverage gate runs each test binary itself rather than trusting a summary.

## What this does not establish

Automation is a local harness invoked by the operator/agent; scheduling and triggering are not automated (no CI, see GEN-081).

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
