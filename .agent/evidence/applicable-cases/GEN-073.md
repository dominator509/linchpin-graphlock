# GEN-073 -- Dependency Integrity Verification

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/dependency-audit exists (lockfile-pinned dependency integrity); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- first-party source `scripts/lint.sh` -- quoted and verified: '--locked'
- first-party source `deny.toml` -- quoted and verified: 'unknown-registry'
- `cargo deny check sources` -- exit 0, output sha256 `5cba1c1c8bf7b4f0...`, 11 bytes (log `deny-sources.log`); asserted: 'sources ok'
- `cargo deny check bans` -- exit 0, output sha256 `60b3148112307d64...`, 111713 bytes (log `deny-bans.log`); asserted: exit status only

## What this establishes

Dependency integrity executed: every enforced build and test lane passes `--locked` so resolution cannot drift from Cargo.lock, cargo-deny verifies crate sources and bans, and the JS graph is installed with a frozen lockfile; the clean-build gate proves the committed locks alone build the workspace.

## What this does not establish

Hash-level verification of downloaded crates is delegated to Cargo's own checksum verification rather than an independent integrity check, and no signature/attestation verification is performed on registry metadata.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
