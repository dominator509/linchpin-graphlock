# GEN-052 -- Broken Access Control Testing

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/test-unit exists (broken access control on workspace paths); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- architecture probe -- `mcp_capability_grant_hits: 11` (captured in `probes.log`)
- architecture probe -- `disclosure_firewall_hits: 42` (captured in `probes.log`)
- first-party source `apps/desktop/src-tauri/src/commands.rs` -- quoted and verified: 'test_mcp_capability_grant_is_explicit', 'test_export_firewall_blocks_restricted_content'
- first-party source `crates/storage/src/vault.rs` -- quoted and verified: 'test_events_are_workspace_scoped', 'test_unknown_workspace_is_refused'
- first-party source `crates/mcp_hub/src/lib.rs` -- quoted and verified: 'test_mcp_grants_and_boundaries'

## What this establishes

Broken-access-control testing executed for the access controls this product actually has: MCP capability grants are explicit per server/client/workspace/capability and deny by default (an unknown capability and an empty grant list are both refused), the disclosure firewall blocks export of restricted content, and workspace-scoped reads do not expose another workspace's events.

## What this does not establish

There is no multi-user or remote access-control model to break, so this ID is satisfied by the grants and the firewall rather than by role enforcement.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
