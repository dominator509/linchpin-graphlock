# GEN-077 -- Binary Integrity Verification

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/artifact-identity exists (artifact digest recomputation); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- evidence artifact `.agent/verification/reports/RELEASE_GATE.json` -- quoted and verified: 'executable_sha256', 'msi_sha256'
- evidence artifact `.agent/evidence/artifact-e2e/STATUS.md` -- quoted and verified: 'SHA-256'
- first-party source `scripts/smoke-installed-artifact.sh` -- quoted and verified: 'msiexec'

## What this establishes

Binary integrity verification executed: the release gate pins the SHA-256 and byte size of both the executable and the MSI, the exact-artifact lane records the digest of the binary it drives, and the installer lane installs the real MSI and verifies the installed artifact.

## What this does not establish

The digests are recorded and checked against the built artifacts, but the release is UNSIGNED (documented limitation in ADR-003), so integrity rests on published digests rather than on an Authenticode signature or attestation.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
