# GEN-006 -- Credential Scanning

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/secret-scan exists (credential patterns in tracked sources); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- `python3 scripts/secret-scan.py --check` -- exit 0, output sha256 `c4842551ef6d5a06...`, 45 bytes (log `secret-scan.log`); asserted: 'secret-scan check: ok (9 rules, no findings)'
- first-party source `crates/crash_reporter/src/lib.rs` -- quoted and verified: 'redact_key_values'
- architecture probe -- `concern[redaction]_tests: 21` (captured in `probes.log`)
- evidence artifact `.agent/evidence/fuzz/STATUS.md` -- quoted and verified: 'panic'
- evidence artifact `.agent/evidence/mutation-proof/REPORT.json` -- quoted and verified: 'MUT-OPS-037'

## What this establishes

Credential scanning executed at two layers: the repository-wide scan, and the product's own redaction policy (21 redaction/secret/credential tests), whose discrimination is proven by mutation MUT-OPS-037 -- removing the key-value redaction pass makes the guarding test fail.

## What this does not establish

Runtime redaction only removes values it can attribute to a key name or a known token shape; unlabelled credential material pasted as free text is not detectable by either layer.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
