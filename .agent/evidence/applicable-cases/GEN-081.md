# GEN-081 -- CI/CD Pipeline Security Testing

**Status: PARTIAL** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/harness-validate exists (harness gate enforcement); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- architecture probe -- `ci_config[.github/workflows]: False` (captured in `probes.log`)
- architecture probe -- `ci_config[.gitlab-ci.yml]: False` (captured in `probes.log`)
- architecture probe -- `ci_config[Jenkinsfile]: False` (captured in `probes.log`)
- `sh scripts/security-check.sh` -- exit 0, output sha256 `65d62f1e7734e156...`, 124735 bytes (log `security-check.log`); asserted: 'security-check: ok'

## What this establishes

The security gates that a pipeline would run are executed and enforced locally, by exit code, in one composed ladder: secret scan, SBOM currency, cargo-deny advisories/bans/sources/licences, pnpm audit, lint, and the unit/integration/artifact lanes.

## What this does not establish

NOT PASS: no hosted CI configuration exists in this repository (measured: no GitHub Actions, GitLab, Jenkins, CircleCI or Woodpecker config), so nothing prevents a merge that bypasses these gates on a machine that does not run them, and there is no pipeline-runner security posture (secrets scoping, runner isolation, artifact provenance) to test.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
