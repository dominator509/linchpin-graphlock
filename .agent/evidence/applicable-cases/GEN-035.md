# GEN-035 -- Desktop Application Security Testing

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/test-e2e exists (desktop application security at the WebView boundary); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- evidence artifact `.agent/evidence/artifact-e2e/STATUS.md` -- quoted and verified: 'linchpin-desktop-e2e.exe', 'DevTools endpoint', 'tauri.localhost'
- first-party source `scripts/artifact-e2e.sh` -- quoted and verified: 'does NOT open a debug port'
- first-party source `apps/desktop/src-tauri/src/commands.rs` -- quoted and verified: 'test_export_firewall_blocks_restricted_content'

## What this establishes

Desktop application security testing executed against the packaged executable: the loaded origin is the embedded frontend rather than a dev server, the production artifact is asserted to open no debug port, and the confidentiality boundary (disclosure firewall, export blocking) is exercised.

## What this does not establish

There is no third-party penetration test and no fuzzing of the WebView2 attack surface; the debug-port assertion is a liveness-checked probe of the exact artifact, not a full network attack simulation.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
