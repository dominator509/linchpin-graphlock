# GEN-112 -- Security Baseline Validation

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/preflight exists (baseline validation before every run); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- evidence artifact `.agent/evidence/clean-build/STATUS.md` -- quoted and verified: '| Verdict |', 'Checkout'
- evidence artifact `.agent/evidence/clean-build/ENVIRONMENT.json` -- quoted and verified: 'toolchain'
- `cargo fmt --all -- --check` -- exit 0, output sha256 `e3b0c44298fc1c14...`, 0 bytes (log `fmt.log`); asserted: exit status only
- `sh scripts/lint.sh` -- exit 0, output sha256 `85b382dadd552896...`, 1863 bytes (log `lint.log`); asserted: 'lint: Done'
- `cargo deny check licenses` -- exit 0, output sha256 `f4ca9ef7f07f2573...`, 1365 bytes (log `deny-licenses.log`); asserted: 'licenses ok'

## What this establishes

Security baseline validation executed: the clean-environment gate materialises an ephemeral checkout with no build output and no installed packages, builds it from the committed locks, runs the whole Rust suite there and proves teardown; format, lint and licence baselines are enforced lanes.

## What this does not establish

The baseline is a fresh checkout of the same host and toolchain, not a hardened or minimal OS image, so it proves freedom from local state rather than a hardened configuration.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
