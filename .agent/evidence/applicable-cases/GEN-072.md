# GEN-072 -- Software Bill of Materials (SBOM) Generation and Verification

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/generate-sbom exists (CycloneDX 1.5 SBOM generation); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- `python3 scripts/generate-sbom.py --check` -- exit 0, output sha256 `ab1c5d4950dc209f...`, 41 bytes (log `sbom-currency.log`); asserted: 'sbom check: ok (720 components, current)'
- `sh scripts/security-check.sh` -- exit 0, output sha256 `65d62f1e7734e156...`, 124735 bytes (log `security-check.log`); asserted: 'security-check: ok'
- evidence artifact `.agent/evidence/sbom/linchpin.cdx.json` -- quoted and verified: '"bomFormat"', '"CycloneDX"', '"licenses"', 'purl'

## What this establishes

SBOM generation and verification executed: CycloneDX 1.5 with 720 components and purls on every component, generated from the committed locks by scripts/generate-sbom.py, with a currency check enforced as a lane (it fails if the SBOM drifts from the locks). An earlier defect in which 0 of 720 components carried a purl was measured and fixed.

## What this does not establish

The SBOM covers Rust and npm components; OS-level and toolchain components are recorded in the environment manifest rather than the SBOM, and no VEX document accompanies it.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
