# GEN-072 -- Software Bill of Materials (SBOM) Generation and Verification

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/generate-sbom exists (CycloneDX 1.5 SBOM generation); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- `python3 scripts/generate-sbom.py --check` -- exit 0, output sha256 `4a6f33a7590f4da4...`, 41 bytes (log `sbom-currency.log`); asserted: 'sbom check: ok (771 components, current)'
- `sh scripts/security-check.sh` -- exit 0, output sha256 `d62acc30e02fc29f...`, 169625 bytes (log `security-check.log`); asserted: 'security-check: ok'
- evidence artifact `.agent/evidence/sbom/linchpin.cdx.json` -- quoted and verified: '"bomFormat"', '"CycloneDX"', '"licenses"', 'purl'

## What this establishes

SBOM generation and verification executed: CycloneDX 1.5 with 771 components and purls on every component, generated from the committed locks by scripts/generate-sbom.py, with a currency check enforced as a lane (it fails if the SBOM drifts from the locks). The count moves with the dependency graph and is quoted from the gate's own output rather than remembered: it was 720 before the dependency upgrade campaign (vitest 5, vite 8, @vitejs/plugin-react 6, eslint 10, typescript 6, sha2 0.11, windows 0.62) and is 771 after it. An earlier defect in which 0 of the components carried a purl was measured and fixed.

## What this does not establish

The SBOM covers Rust and npm components; OS-level and toolchain components are recorded in the environment manifest rather than the SBOM, and no VEX document accompanies it.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
