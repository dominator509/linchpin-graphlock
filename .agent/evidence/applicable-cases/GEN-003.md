# GEN-003 -- Dependency Scanning

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/dependency-audit exists (cargo-deny advisories/bans); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- `cargo deny check advisories` -- exit 0, output sha256 `799fc05e6513bf36...`, 14 bytes (log `deny-advisories.log`); asserted: 'advisories ok'
- `pnpm audit --audit-level high` -- exit 0, output sha256 `15950a68a7ed99c5...`, 31 bytes (log `pnpm-audit.log`); asserted: exit status only
- evidence artifact `.agent/evidence/sbom/linchpin.cdx.json` -- quoted and verified: '"bomFormat"', '"CycloneDX"'

## What this establishes

Dependency scanning executed for both ecosystems against committed locks, and the resulting component inventory is materialised as a CycloneDX SBOM with 720 components.

## What this does not establish

Scanning is per-candidate, not continuous: no scheduled re-scan runs between candidates, so an advisory published later is only seen on the next run.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
