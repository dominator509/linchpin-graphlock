# E2E-013 -- Version-Skew & Compatibility Verification

**Status: PARTIAL** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: E2E
- Applicability: APPLICABLE -- end-to-end orchestrator pack: executable harness entry point scripts/version-matrix exists (two released versions exist and are installed in sequence; simultaneous mixed-fleet skew is not applicable to a device-local single-user product); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- evidence artifact `.agent/evidence/version-matrix/report.json` -- quoted and verified: 'applicability_notes', 'mixed-fleet', 'versions'
- first-party source `scripts/version-matrix.py` -- quoted and verified: 'mixed-fleet', 'payload_exe_sha256'

## What this establishes

Version-skew verification is PARTIAL and the missing half is structural rather than pending work: two RELEASED versions exist (v0.1.0 and v0.2.0, each with a distinct MSI and a distinct binary payload digest) and the matrix installs each in sequence with the installed version read back from the Windows Installer registry, so the transitions 0.1.0 -> 0.2.0 -> 0.1.0 are measured rather than assumed.

## What this does not establish

NOT PASS: simultaneous mixed-fleet skew -- two different versions running at once against shared state -- is NOT APPLICABLE to this product and is recorded as such in the evidence, because it is a single-user desktop application with device-local storage, no server fleet and no shared state, so no such path exists to execute. The two releases also differ only in their version manifests, so behavioural skew between versions is not exercised.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
