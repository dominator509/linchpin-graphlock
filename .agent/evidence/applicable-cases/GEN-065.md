# GEN-065 -- Configuration Hardening Validation

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/test-unit exists (configuration hardening: release config refuses unknown keys and missing required values); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- first-party source `crates/platform_windows/src/lib.rs` -- quoted and verified: 'test_unknown_keys_warn_in_development_and_fail_in_release'
- first-party source `crates/platform_windows/src/lib.rs` -- quoted and verified: 'test_missing_required_value_is_an_error_not_a_fallback'
- first-party source `crates/platform_windows/src/lib.rs` -- quoted and verified: 'test_release_runtime_is_parsed'
- evidence artifact `.agent/evidence/clean-build/ENVIRONMENT.json` -- quoted and verified: 'toolchain'

## What this establishes

Configuration hardening IS asserted, which this row denied: the configuration surface parses its environment ONCE into typed values, unknown keys are a warning in development and a HARD FAILURE in release, and a missing required value is an error rather than a fallback -- the fail-closed direction the hardening clause asks for. The build environment itself is pinned and recorded (toolchain, lockfile digests) by the clean-build gate.

## What this does not establish

There is no CIS/STIG-style baseline and no scanner that compares the installed configuration against a policy; the hardening claim is bounded to the product's own configuration surface and to the release-mode behaviour its tests assert.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
