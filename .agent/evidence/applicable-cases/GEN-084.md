# GEN-084 -- Post-Build Security Testing

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/artifact-identity exists (post-build artifact verification by digest); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- evidence artifact `.agent/evidence/artifact-e2e/STATUS.md` -- quoted and verified: 'linchpin-desktop-e2e.exe', 'SHA-256'
- first-party source `scripts/smoke-installed-artifact.sh` -- quoted and verified: 'msiexec'
- first-party source `scripts/artifact-e2e.sh` -- quoted and verified: 'does NOT open a debug port'

## What this establishes

Post-build security testing executed on the produced artifact rather than on source: the packaged executable is driven over its real WebView2 instance, asserted to open no debug port, and the installer lane installs, launches and removes the actual MSI.

## What this does not establish

The post-build checks are behavioural assertions (origin, debug port, confirmation gating, redaction); no binary-hardening audit (ASLR/CFG/DEP flags, symbol stripping, dependency signature check) is performed on the linked image.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
