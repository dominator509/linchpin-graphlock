# GEN-014 -- Cyclomatic Complexity Analysis

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/code-metrics exists (cyclomatic complexity per function with an enforced production ceiling and a hotspot list); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- first-party source `scripts/code-metrics.py` -- quoted and verified: 'MAX_FUNCTION_COMPLEXITY', 'DECISION_TOKENS', 'def functions'
- evidence artifact `.agent/evidence/code-metrics/STATUS.md` -- quoted and verified: 'Complexity hotspots', 'max complexity'
- evidence artifact `.agent/evidence/code-metrics/report.json` -- quoted and verified: 'max_complexity_production', 'hotspots'

## What this establishes

Cyclomatic complexity is measured and ceilinged, which this row denied. Every first-party function gets 1 + its decision points (`if`, `else if`, match arms, `&&`, `||`, `for`, `while`, `loop`, `?`), production functions must stay at or below 40, and the ranked hotspot list is printed in the evidence. MEASURED: 353 production functions with a maximum of 35 and a mean of 3.9, and 57 test-file functions with a maximum of 43 that are measured and printed but exempt from the ceiling. DISCRIMINATION IS PROVEN: --self-test measures a synthetic function with twenty `if` conditions and twenty `&&` operators and asserts the harness reports a complexity of 41, alongside a planted `unsafe` block and a `todo!`.

## What this does not establish

The measurement counts decision TOKENS; it does not build a control-flow graph, so it under-counts some shapes (early returns, macros, iterator chains) and cannot distinguish nesting that makes a function hard to read from branching that does not. It is a review-ranking and a ratchet, not a complexity proof.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
