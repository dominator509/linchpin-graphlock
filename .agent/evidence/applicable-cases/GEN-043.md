# GEN-043 -- Vulnerability Assessment

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/assess-advisories exists (per-advisory assessment of the shipped graph: decision, reachability, owner, observed paths); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- first-party source `scripts/assess-advisories.py` -- quoted and verified: 'def rust_advisories', 'a live finding needs FIX, MITIGATE, ACCEPT or NOT_REACHABLE'
- evidence artifact `.agent/evidence/advisory-assessment/STATUS.md` -- quoted and verified: 'Advisories and decisions', 'Rationale per advisory', 'What this gate refuses'
- evidence artifact `.agent/verification/state/ADVISORY_ASSESSMENTS.json` -- quoted and verified: 'observed_paths', 'reachability'
- `cargo deny check advisories` -- exit 0, output sha256 `799fc05e6513bf36...`, 14 bytes (log `deny-advisories.log`); asserted: 'advisories ok'

## What this establishes

Vulnerability assessment now runs, which is what this row denied: a scan FINDS, an assessment DECIDES. scripts/assess-advisories.py normalizes what the two scanners report (`cargo deny --format json check advisories`, `pnpm audit --json`) and binds each finding to an authored entry carrying the decision, the REACHABILITY fact that decides it (devDependency path versus shipped artefact), the owner, and the dependency paths it was assessed against. On this candidate: cargo-deny reports no advisories; pnpm audit reports two moderate advisories, BOTH reached only through the vitest devDependency tree ('.>vitest' and '.>vitest>@vitest/mocker'), so both are assessed ACCEPT with the reachability stated and the deferral of the vitest upgrade recorded. The harness FAILS when a scanner reports an advisory with no decision, when a decision lacks a rationale, reachability statement or owner, when an ACCEPT entry's observed paths no longer match the scan (the dependency moved, so the assessment is stale), when a LIVE advisory is filed as RESOLVED, and when the register carries a decision about an advisory nothing reports any more.

## What this does not establish

The assessment is reachability-based, not exploitability-based: it establishes where the vulnerable package sits in the graph and whether it ships, not whether a specific vulnerable code path can be driven in this configuration. No CVE severity scoring of our own, no vendor patch tracking, and no SBOM-to-advisory correlation beyond the two scanners.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
