# E2E-014 -- Downgrade & Rollback Compatibility Verification

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: E2E
- Applicability: APPLICABLE -- end-to-end orchestrator pack: executable harness entry point scripts/version-matrix exists (downgrade and rollback executed against realistic persistent state (DOD-035)); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- evidence artifact `.agent/evidence/version-matrix/report.json` -- quoted and verified: 'downgrade_attempt', 'rollback', 'matches_shipped_payload', 'state_intact'
- evidence artifact `.agent/evidence/version-matrix/STATUS.md` -- quoted and verified: 'downgrade_attempt', 'rollback'
- first-party source `scripts/version-matrix.py` -- quoted and verified: 'refused by Windows Installer', 'consistent_with_outcome'

## What this establishes

Downgrade and rollback compatibility executed against realistic persistent state: the in-place downgrade from v0.2.0 to v0.1.0 was APPLIED by this installer (exit 0, version back to 0.1.0, shipped-payload digest matched, canary database and blob byte-intact), and the rollback path (uninstall, then reinstall v0.1.0) reconciled the same state. The assertion accepts either installer outcome but requires the installed version to AGREE with the measured exit code, so a refusal with no version change also passes while an exit 0 with the wrong version does not.

## What this does not establish

The two releases carry the same code with different version manifests, so this proves installer-level downgrade and rollback behaviour with realistic state, not schema downgrade behaviour -- there is no schema to downgrade because no migration was added between the releases.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
