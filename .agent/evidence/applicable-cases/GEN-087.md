# GEN-087 -- Security Gate Enforcement

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/ship-gate exists (release gate enforcement); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- `sh scripts/security-check.sh` -- exit 0, output sha256 `d62acc30e02fc29f...`, 169625 bytes (log `security-check.log`); asserted: 'security-check: ok'
- `python3 scripts/secret-scan.py --check` -- exit 0, output sha256 `c4842551ef6d5a06...`, 45 bytes (log `secret-scan.log`); asserted: 'secret-scan check: ok (9 rules, no findings)'
- evidence artifact `.agent/verification/reports/RELEASE_GATE.json` -- quoted and verified: 'blocking_clauses'
- first-party source `scripts/ship-gate.py` -- quoted and verified: 'NO_GO'

## What this establishes

Security gate enforcement executed by exit code, not intention: every scanner lane reports its own status and the gate exits non-zero if any lane fails, one failing lane cannot be masked by a later passing one, and the release verdict is computed from measured state (currently NO_GO with the blocking clause recorded).

## What this does not establish

Enforcement binds only processes that invoke the ladder; there is no CI runner or branch protection to make it unbypassable (see GEN-081).

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
