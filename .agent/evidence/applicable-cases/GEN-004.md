# GEN-004 -- License Compliance Scanning

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/dependency-audit exists (cargo-deny license policy); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- `cargo deny check licenses` -- exit 0, output sha256 `f4ca9ef7f07f2573...`, 1365 bytes (log `deny-licenses.log`); asserted: 'licenses ok'
- `sh scripts/security-check.sh` -- exit 0, output sha256 `d62acc30e02fc29f...`, 169625 bytes (log `security-check.log`); asserted: 'security-check: ok'
- evidence artifact `.agent/evidence/sbom/linchpin.cdx.json` -- quoted and verified: '"licenses"'
- first-party source `deny.toml` -- quoted and verified: 'MPL-2.0'

## What this establishes

Licence compliance executed: `cargo deny check licenses` exits 0 against an explicit allowlist (MPL-2.0 permitted under ACCEPTED ADR-002), and the SBOM generator enforces the same allowlist so the scanner and the inventory cannot disagree.

## What this does not establish

Four allowlist entries were never encountered in the graph (Apache-2.0 WITH LLVM-exception, BSD-2-Clause, ISC, Unicode-DFS-2016), so the policy is broader than the measured graph requires; licence classification comes from crate metadata, not from a legal review of texts.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
