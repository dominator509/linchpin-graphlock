# GEN-057 -- Cryptographic Implementation Testing

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/test-unit exists (cryptographic implementation tests: SHA-256 vector, update signature verification, encrypted container); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- first-party source `crates/storage/src/vault.rs` -- quoted and verified: 'test_sha256_known_vector'
- first-party source `crates/storage/src/lib.rs` -- quoted and verified: 'test_encrypted_vault'
- first-party source `crates/evidence/src/lib.rs` -- quoted and verified: 'test_update_signature_verification'
- architecture probe -- `concern[redaction]_tests: 21` (captured in `probes.log`)

## What this establishes

Cryptographic implementation IS tested, which this row denied. Executed: a SHA-256 known-vector test (so the hash is checked against a published value rather than against itself), an encrypted-blob container round trip, update-signature verification, content-addressed blob storage whose address is the digest of the content, and the threat-control tests that assert the secret allowlist holds no cryptographic material of its own.

## What this does not establish

There is no cryptographic review by a specialist and no FIPS/algorithm-agility requirement to test against; the primitives are those of the pinned dependencies (SHA-256, the encrypted container's cipher), so the tests prove the product uses them correctly at the boundaries it exposes, not that the primitives themselves are sound.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
