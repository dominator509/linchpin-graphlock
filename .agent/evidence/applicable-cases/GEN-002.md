# GEN-002 -- Software Composition Analysis (SCA)

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/security-check exists (cargo-deny supply-chain gate); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- `cargo deny check advisories` -- exit 0, output sha256 `799fc05e6513bf36...`, 14 bytes (log `deny-advisories.log`); asserted: 'advisories ok'
- `cargo deny check bans` -- exit 0, output sha256 `60b3148112307d64...`, 111713 bytes (log `deny-bans.log`); asserted: exit status only
- `cargo deny check sources` -- exit 0, output sha256 `5cba1c1c8bf7b4f0...`, 11 bytes (log `deny-sources.log`); asserted: 'sources ok'
- `pnpm audit --audit-level high` -- exit 0, output sha256 `15950a68a7ed99c5...`, 31 bytes (log `pnpm-audit.log`); asserted: 'No known vulnerabilities found'

## What this establishes

Composition analysis executed against the locked graphs of both ecosystems: cargo-deny advisories/bans/sources and `pnpm audit`, all exit 0. The npm audit reports no known vulnerabilities at all, not merely none above the enforced threshold: the two moderate advisories it used to report (GHSA-82fw-gwwq-j7x9 through vitest and @vitest/mocker) were REMEDIATED by the vitest upgrade (3.2.7 -> 5.0.1 via Dependabot PRs #7 and #11), and the register entries that assessed them are marked RESOLVED because no scanner reports them any more.

## What this does not establish

Scanning is per-candidate, not continuous in this harness: an advisory published after this run is only seen on the next one. (Continuous coverage is what the repository's Dependabot alerts and weekly version updates provide, and they are configured separately.)

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
