# E2E-008 -- Performance & Workload Orchestration

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: E2E
- Applicability: APPLICABLE -- end-to-end orchestrator pack: executable harness entry point scripts/performance-lane exists (performance workload with encoded thresholds (DOD-022)); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- evidence artifact `.agent/evidence/performance/report.json` -- quoted and verified: '"verdict": "PASS"', 'p95_write_ms', 'total_write_ms', 'limits'
- first-party source `apps/desktop/src-tauri/tests/performance_gate.rs` -- quoted and verified: 'p95', 'assert'
- first-party source `scripts/performance-lane.sh` -- quoted and verified: 'performance_gate'

## What this establishes

Performance workload verification executed through its own runnable entry point (`scripts/performance-lane.sh`): 200 conception events written one at a time through the production command into a fresh file-backed vault, every event read back through a SECOND connection, environment recorded, and encoded bounds (p50 50ms, p95 150ms, max 500ms, total write 30s, total read 2s, vault 32MiB) that FAIL the run rather than reporting an observation. The read is asserted to observe every write, so a fast but lossy path cannot pass by dropping work.

## What this does not establish

The profile is debug and the workload is single-machine, single-process and concurrency-free, so the numbers are regression bounds on this host rather than published service levels; no cost metric is encoded because the product has no metered resource. The pack specifies no numeric performance scale, so the trial defines and labels its own workload.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
