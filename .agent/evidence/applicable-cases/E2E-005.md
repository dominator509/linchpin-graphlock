# E2E-005 -- Regression & Differential Verification

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: E2E
- Applicability: APPLICABLE -- end-to-end orchestrator pack: executable harness entry point scripts/test-e2e exists (regression and differential verification lane); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- evidence artifact `.agent/verification/state/RERUN_RECORD.json` -- quoted and verified: 'epoch_digest'
- first-party source `scripts/change-invalidation.py` -- quoted and verified: 'changed_classes'
- evidence artifact `.agent/evidence/mutation-proof/REPORT.json` -- quoted and verified: 'MUT-OPS-037'

## What this establishes

Regression and differential verification executed: a change to any tracked input class advances the epoch and obliges a rerun of the affected stages, recorded with commands, exit codes and output digests, while the mutation harness supplies the differential half by proving the guarded behaviour fails when the guard is removed.

## What this does not establish

Differential comparison is against the guard's absence, not against a previous release build; no historical version-to-version differential run exists (see DOD-035).

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
