# E2E-019 -- AI/Agentic Systems Safety & Capability Verification

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: E2E
- Applicability: APPLICABLE -- end-to-end orchestrator pack: executable harness entry point scripts/live-fire-local-provider exists (AI/agentic capability plus fail-closed safety negatives); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- evidence artifact `.agent/evidence/local-provider/STATUS.md` -- quoted and verified: 'smollm2:135m', 'transport identity is the local adapter', 'non-loopback endpoint is refused'
- first-party source `apps/desktop/e2e-local-provider.mjs` -- quoted and verified: 'canary'
- first-party source `crates/provider_transport/src/lib.rs` -- quoted and verified: 'test_local_adapter_rejects_non_loopback_endpoint'
- first-party source `apps/desktop/src-tauri/src/commands.rs` -- quoted and verified: 'test_local_inference_refuses_non_loopback_endpoint'
- architecture probe -- `loopback_guard_hits: 66` (captured in `probes.log`)
- architecture probe -- `concern[provider]_tests: 9` (captured in `probes.log`)

## What this establishes

AI/agentic safety verification executed: the provider lane drives the product's inference command against a real loopback model and asserts a live completion with a runtime canary that must not already exist in the tracked tree, proves the fail-closed negatives (non-loopback endpoint refused with POLICY and no attempt; unreachable endpoint yields a transport error rather than fabricated text), labels AI-origin content distinctly from human conception, and denies MCP capabilities by default.

## What this does not establish

One small local model is exercised; no adversarial prompt-injection campaign is run against the model, and model-output trustworthiness is bounded by the labelling and the disclosure firewall rather than by output verification.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
