# GEN-063 -- Logging and Monitoring Verification

**Status: PASS** (recorded in `.agent/verification/state/CASE_RESULTS.json`)

- Source group: General
- Applicability: APPLICABLE -- general application/security pack: executable harness entry point scripts/security-check exists (logging/redaction verification via RedactionReport); case not yet executed against a pinned candidate.
- Generated by `scripts/applicable-case-evidence.py --write`; re-verified by `--check`,
  which fails if any quoted line, symbol or artifact below no longer says what is quoted.

## Executed check(s)

- evidence artifact `.agent/evidence/DOD-037-operator-diagnostics.md` -- quoted and verified: 'alert', 'correlation', 'DEGRADED'
- first-party source `apps/desktop/src-tauri/src/commands.rs` -- quoted and verified: 'diagnostics_redactor', 'record_diagnostic_outcome'
- architecture probe -- `concern[diagnostics]_tests: 16` (captured in `probes.log`)

## What this establishes

Logging and monitoring verification executed against induced failures: the diagnostics surface reports readiness measured by probing storage, records every command outcome with measured duration and a correlation id, derives alerts from those measurements, and redaction is asserted on the same path with 16 diagnostics/health tests.

## What this does not establish

Telemetry is device-local with no exporter, so there is no central monitoring stack to verify; alert routing beyond the in-app surface is not implemented.

## Raw captures

- Gate logs: `.agent/evidence/applicable-cases/logs/` (see `RUNS.json` for argv, exit
  codes and output digests)
- Architecture probes: `.agent/evidence/applicable-cases/logs/probes.log`
