# LINCHPIN GraphLock Blueprint Pack

This directory is the generated project-specific FORGE output. It is intentionally not a claim that LINCHPIN product code exists. Start a coding-agent session in a new Git repository containing this pack, instruct it to follow AGENTS.md, run `sh scripts/preflight.sh`, and execute the graph. EP-000 must re-verify provider terms, patent workflow rules, tool versions and licenses before implementation.

Deep research and architecture rationale are in `RESEARCH_NOTES.md`, `ARCHITECTURE.md`, `PROVIDER_TRANSPORT_MATRIX.md`, `PATENT_DOMAIN_MODEL.md`, and `LINCHPIN_SCORING_SPEC.md`.
